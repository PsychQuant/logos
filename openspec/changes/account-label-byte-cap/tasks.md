## 1. Mutation-path caps (RED → GREEN)

- [x] 1.1 [P] Author failing mutation-path tests in `Tests/LogoSwitchTests/AccountTests.swift` proving the label contract from the "Registry mutations validate labels" requirement is unmet today: `Account.validate(label:)` and `Account(label:)` must reject/clamp a within-30-graphemes-but-over-256-bytes label (a base char + ~4000 combining marks "zalgo", a 30× ZWJ family-emoji string, and a flag/CJK mix over budget). Assert `validate` throws `ValidationError.labelTooLong` ("Reuse labelTooLong rather than adding a ValidationError case"), and that an interior/trailing-newline label trims via `.whitespacesAndNewlines` ("Unify the trim set on .whitespacesAndNewlines"). Verification: `swift test` shows these named tests FAIL (RED) before task 1.2.
- [x] 1.2 Deliver the shared bound so the mutation gate enforces both caps: add `static let maxLabelUTF8Bytes = 256` ("256-byte UTF-8 budget as a named constant") plus a shared normalizing core and a throwing wrapper on `Account` ("Shared normalizing core plus a throwing validate wrapper") in `Sources/LogosAccounts/Account.swift`; the core clamps on a `Character` boundary ("Grapheme-cluster-aware UTF-8 byte truncation") and trims `.whitespacesAndNewlines` ("Unify the trim set on .whitespacesAndNewlines"); route `validate(label:)` (throws on either cap) and `init(label:)` (byte-clamps only, per "Construction byte-clamps only (init preserves grapheme length)") through the shared primitives. Verification: the task 1.1 tests now PASS (GREEN) and `swift build` is clean.

## 2. Load-repair path (RED → GREEN)

- [x] 2.1 [P] Author failing load-repair tests in `Tests/LogosAccountsTests/AccountRegistryTests.swift` proving `AccountRegistry.normalize()` clamps a persisted over-budget label (decode-path bypass) to `utf8.count <= 256` on a whole-grapheme boundary with no split cluster, AND that a clean fat-but-in-budget index is byte-unchanged after load ("Fold the byte clamp into normalize's net-change comparison" — net-change convergence preserved). Verification: `swift test` shows these named tests FAIL/record (RED) before task 2.2.
- [x] 2.2 Route `AccountRegistry.normalize()` phase 3 through the shared `Account` helper so the load-time repair path clamps over-budget labels on a grapheme boundary, with the byte clamp folded into the end-of-pass `labelsBefore` net comparison ("Fold the byte clamp into normalize's net-change comparison") so a clean index is never re-saved. Verification: the task 2.1 tests now PASS (GREEN); the full `swift test` suite is green except the documented `WorkspaceLoader loadAsync` timing flake.

## 3. Grapheme-safety + byte-bound assertions

- [x] 3.1 Assert the safety invariant across every path: extend the tests from 1.1 and 2.1 so each persisted/returned label asserts BOTH `label.utf8.count <= Account.maxLabelUTF8Bytes` AND that the label round-trips as valid `Character`s (mapping `Array(label)` back to a `String` equals the label — no mid-scalar cut, no dangling ZWJ), covering the mid-cluster boundary case where a raw `utf8.prefix` would split ("Grapheme-cluster-aware UTF-8 byte truncation"). Verification: `swift test` — the boundary assertions pass.

## 4. User-visible changelog

- [x] 4.1 Record the user-visible behavior change in `CHANGELOG.md`: a label previously accepted/persisted (within 30 grapheme clusters but over 256 UTF-8 bytes) is now rejected on create/rename or silently clamped on load, and the trim set is now `.whitespacesAndNewlines`. Verification: content review — the entry names both the rejection (mutation path) and the clamp (load path) and the 256-byte cap.
