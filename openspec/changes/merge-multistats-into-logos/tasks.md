## 1. Repo merge and target scaffold

- [x] 1.1 Merge the MultiStats repo into logos with commit history preserved, per design "Decision 5: History-preserving subtree merge". Behavior: imported sources are reachable in the logos tree and their pre-merge history is intact. Verify: git log --follow on an imported file (e.g. the Keychain reader source) lists original MultiStats commits; swift build still succeeds.
- [x] 1.2 Restructure the merged code into in-repo targets per design "Decision 1: In-repo targets over submodule or remote SPM dependency" and "Decision 2: Target layering — LogosAccounts / LogosUsage / MultiStats executable". Behavior: Package.swift declares LogosAccounts (Foundation-only library), LogosUsage (library, depends on LogosAccounts), and the MultiStats executable (depends on LogosUsage); LogoSwitch depends on LogosAccounts; the Logos app depends on LogosAccounts and LogosUsage. Verify: swift build produces the Logos and MultiStats executables; swift test runs the relocated MultiStatsCore unit tests (keychain parsing, service-name derivation, discovery filtering) green under the new target names.

## 2. Shared registry (LogosAccounts)

- [x] 2.1 Implement AccountRegistry so the "Account list persists in a shared file-based index" (version-1 JSON at the accounts root, atomic temp-plus-rename writes) and "Registry mutations validate labels" (non-empty, max 30 chars, no duplicate of another account, own-label rename allowed, id and createdAt preserved on rename), per design "Decision 3: Registry persistence moves to a shared JSON index file with one-time UserDefaults migration". Verify: round-trip unit test — create/rename/remove against a temp index file reloads identically; label-validation unit tests cover the example table from the account-registry spec.
- [x] 2.2 Implement the one-time migration so "Legacy per-app account data migrates non-destructively": absent index file plus decodable legacy UserDefaults data produces an index file with identical accounts, the legacy key is never written or deleted, and undecodable legacy data yields an empty registry plus a log notice. Verify: unit tests with a legacy-fixture UserDefaults suite (decodable and undecodable cases) assert index content and untouched legacy data.
- [x] 2.3 Implement corrupt-index handling so "A corrupt index file is never silently destroyed": a parse failure loads an empty registry, records a log notice, and leaves the corrupt file byte-identical until the first explicit user mutation. Verify: unit test writes garbage to the index path, loads the registry, asserts empty list plus unchanged file bytes, then performs a create and asserts the file is rewritten.
- [x] 2.4 [P] Consolidate discovery so "The config-dir convention has a single source of truth": config-dir derivation exists only in LogosAccounts; discovery returns the default account (when its config JSON exists) plus convention accounts sorted by directory name, excluding shell directories without a top-level claude config JSON. Verify: relocated discovery unit tests pass; a grep over Sources/LogoSwitch confirms no remaining config-dir path derivation outside LogosAccounts.

## 3. LogoSwitch slimming

- [x] 3.1 Slim AccountManager to launcher state (active selection, live-401 observation, spawn glue) delegating create/rename/remove/persist to AccountRegistry, keeping "Active selection stays out of the shared registry": activeAccountId remains in Logos-local UserDefaults and switching the active account writes no index file. Verify: existing AccountManager behavior tests pass against the delegating implementation; a new unit test asserts setActive leaves the index file unmodified.

## 4. Red-line audit extension

- [x] 4.1 Extend the red-line audit per design "Decision 4: Red-line audit extends by scope, not by exception" so "Security framework usage is confined to the audited usage target" and "The registry target is structurally credential-free": the original LogoSwitch forbidden-token test stays unchanged and passes; new scans apply the same forbidden-token list to Sources/LogosAccounts and Sources/Logos; a package-wide test asserts a Security import appears only under Sources/LogosUsage. Verify: swift test shows the original LogoSwitch red-line test plus the new scope tests green.
- [x] 4.2 [P] Add the write-token audit so "Keychain writes are structurally forbidden package-wide": no SecItemAdd, SecItemUpdate, or SecItemDelete token in compiled code of any target, including LogosUsage. Verify: the new package-wide write-token audit test passes; temporarily inserting a SecItemAdd call in LogosUsage makes it fail (checked locally, then reverted).

## 5. Usage layer (LogosUsage)

- [x] 5.1 Relocate the credentials reader and usage client preserving that "Keychain credential access is strictly read-only" (SecItemCopyMatching only, no token refresh — expired tokens surface as a display state, unknown expiry defers to the endpoint) and that "Keychain service names follow claude's own derivation" (bare service for the default account, SHA-256 8-hex suffix for per-directory accounts). Verify: relocated unit tests for credential parsing and service-name derivation pass; an expired-token unit test asserts the expired display state with zero refresh attempts against a fake client.
- [x] 5.2 Port AccountUsageModel so "Per-account usage retrieval is isolated" and "Missing or denied credentials surface as a quiet display state": one account's endpoint error or denied Keychain read yields a per-account error/no-credentials state without clearing or blocking other accounts, and no re-read occurs until an explicit refresh. Verify: unit tests with a fake keychain and fake client — mixed success/failure fan-out leaves successes loaded; a denied read performs exactly one lookup per refresh pass.
- [x] 5.3 Serialize the first credential pass so the "First authorization pass serializes Keychain reads": on the first refresh after launch, per-account Keychain reads run one at a time so first-run authorization dialogs never stack. Verify: unit test with an instrumented fake keychain asserts maximum one in-flight read during the first pass and restored concurrency afterwards.

## 6. Logos usage window

- [ ] 6.1 Add the usage window per design "Decision 6: Logos usage window is a consumer, not a discoverer" so "The Logos usage window renders the registry" (label, identity when known, plan-usage consumption, credential state; refresh on open and on explicit action; display-only — no switch, no login, no registry mutation) and "The shared bare Keychain entry is never mutated" holds for it (every window read uses a hash-suffixed service name; the bare entry is never read or written by the window). Verify: unit test feeds a registry fixture into the window model and asserts listed accounts equal the registry with only hash-suffixed service lookups; manual assertion — the window lists the same accounts as Settings -> Accounts and offers no mutating control.

## 7. Standalone viewer

- [ ] 7.1 Keep the thin executable working so "The standalone viewer keeps discovery-based parity": discovery-based account list (default plus convention accounts), same per-account states as the Logos window, registry labels applied when the shared index file exists. Verify: unit test overlays index labels onto discovered accounts; manual smoke — swift run MultiStats on this machine lists the default account plus Logos-convention accounts with usage bars.

## 8. Repo hygiene

- [ ] 8.1 Finish the external migration: transfer the three open MultiStats issues (keychain prompt stacking, usage decode hardening, generation token + redirect hardening) to the logos repo, update the MultiStats README to point at logos as the new home, and archive the MultiStats repo. Verify: the transferred issues are open in logos with their bodies intact; the MultiStats repo shows as archived on GitHub with the pointer README rendered.
