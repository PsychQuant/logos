# macOS
.DS_Store

# Xcode / SwiftPM
.build/
DerivedData/
Package.resolved
.swiftpm/

# XcodeGen — Logos.xcodeproj is a generated build artifact (Track B test host,
# testing-smoke-e2e-strategy). project.yml is the source of truth; regenerate
# with `xcodegen generate`.
*.xcodeproj/
*.xcworkspace/

# Editor
.vscode/
*.code-workspace
*.swp
*~

# Claude Code / superpowers
.claude/
.superpowers/
.remember/

# Spectra (when initialized)
.spectra/

# Local notes
NOTES.md
TODO.local.md
openspec/.vector-search.db*

# Reference repos (cloned for study, never committed)
references/
